---
phase: 08-public-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [PUB-01]

must_haves:
  truths:
    - "Landing page hero has zinc-950 background with Bebas Neue headline and emerald CTA — no white or light backgrounds visible"
    - "Navigation is transparent/blurred on dark and solidifies to zinc-950 on scroll"
    - "Feature cards use zinc-900 backgrounds with white/10 borders and emerald icon accents"
    - "Social proof strip, how-it-works timeline, comparison table, and bottom CTA all use dark surfaces"
    - "Footer is dark-themed with no white backgrounds"
    - "Exit intent popup is dark-themed"
    - "All existing framer-motion animations and functionality (scroll handler, exit intent, lead submission, mobile menu) still work"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dark-themed landing page"
      contains: "bg-background"
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/leads"
      via: "fetch in submitLead"
      pattern: "fetch.*api/leads"
    - from: "src/app/page.tsx"
      to: "framer-motion"
      via: "motion components and useInView"
      pattern: "motion\\."
---

<objective>
Restyle the landing page (src/app/page.tsx) from its current white/light theme to the dark athletic hybrid design.

Purpose: The landing page is the first thing prospects see. It must immediately communicate premium fitness brand — dark, bold, unapologetic. This is the highest-impact visual change in the phase.
Output: Fully dark-themed landing page with Bebas Neue headlines, emerald accents, dark cards, and all existing animations preserved.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-public-pages/08-CONTEXT.md
@.planning/phases/07-design-foundation/07-01-SUMMARY.md
@.planning/phases/07-design-foundation/07-02-SUMMARY.md
@src/app/globals.css
@src/app/page.tsx

<interfaces>
<!-- Phase 7 CSS variables available (from globals.css :root) -->
- bg-background = zinc-950
- bg-card = zinc-900
- bg-primary / text-primary = emerald-500
- text-foreground = white
- bg-muted / bg-secondary / bg-accent = zinc-800ish
- text-muted-foreground = zinc-400ish
- border-border = white/10
- bg-input = white/5
- ring-ring = emerald-500

<!-- Phase 7 utility classes (from globals.css @layer utilities) -->
- .transition-micro — 200ms ease for interactive elements
- .hover-card-glow — scale(1.01) + emerald border glow on hover
- .hover-brightness — brightness(1.1) on hover
- .animate-fade-in-up — fade + slide up entrance
- .animate-count-up — fade + slide for stat numbers

<!-- Font class -->
- font-display — Bebas Neue for headings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle landing page hero, navigation, and upper sections</name>
  <files>src/app/page.tsx</files>
  <action>
Restyle the top half of page.tsx (through the "How It Works" section). Preserve ALL existing state, hooks, event handlers, framer-motion animations, data arrays, and JSX structure. Only change className strings and text content where specified.

**Root container:**
- `min-h-screen bg-white` -> `min-h-screen bg-background`

**Sticky Header (nav):**
- Scrolled state: `bg-white/90 backdrop-blur-lg border-b shadow-sm` -> `bg-zinc-950 backdrop-blur-lg border-b border-border`
- Unscrolled: `bg-transparent` stays
- Logo: Keep `Max<span>Health</span>` but change span to `text-primary` (was text-emerald-600)
- Nav links: `text-zinc-600 hover:text-zinc-900` -> `text-muted-foreground hover:text-foreground`
- "Get Started" button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- Mobile menu: `bg-white border-b` -> `bg-card border-b border-border`
- Mobile menu links: add `text-foreground` class

**Hero section:**
- Remove gradient div: `bg-gradient-to-br from-zinc-50 via-white to-emerald-50/30` -> `bg-gradient-to-b from-background via-background to-zinc-900/50` (subtle gradient)
- Badge: keep as-is (already uses shadcn Badge which is dark-themed)
- h1: Add `font-display` class, increase size. Change text:
  - "Transform Your Body" / "With Science-Backed Coaching" ->
  - "YOUR BODY. YOUR PLAN." (line break) "YOUR RESULTS." in white, with no emerald span (whole thing in white, Bebas Neue handles the impact)
  - Size: `text-5xl sm:text-6xl md:text-7xl lg:text-8xl`
- Subheadline p: `text-zinc-500` -> `text-muted-foreground`, keep copy but change to: "Personalized meal plans, custom training programs, and weekly accountability — all tailored to your body, goals, and lifestyle."
- CTA buttons: Primary "Apply Now" -> `bg-primary hover:bg-primary/90 text-primary-foreground`, text change to "APPLY NOW"
- "Take Free Assessment" outline button: add `border-border text-foreground hover:bg-card`
- Social proof line: Change to "500+ Plans Created | Advanced Protocol Support | NASM Certified" with `text-primary` color (emerald on dark)
- Bouncing chevron: `text-zinc-300` -> `text-muted-foreground`

**Features section:**
- Section bg: remove any light bg (it has none currently, just ensure no bg class)
- Badge, h2, p: `text-zinc-500` -> `text-muted-foreground`. Add `font-display` to h2. Change h2 text to "WHAT YOU GET"
- Feature cards: Replace `hover:shadow-lg hover:border-emerald-200` with `hover-card-glow` utility class
- Card icon container: `bg-emerald-100` -> `bg-primary/10`, `group-hover:bg-emerald-500` -> `group-hover:bg-primary`
- Icon color: `text-emerald-600` -> `text-primary`, `group-hover:text-white` stays
- Card text: `text-zinc-500` -> `text-muted-foreground`
- Update feature titles per CONTEXT: "Custom Macro Targets" -> "Personalized Macros", "Custom Meal Plans" stays, "Training Programs" -> "Periodized Training", "Progress Tracking" -> "Weekly Check-ins", "Photo Comparisons" -> "Supplement Guidance", "Coach Oversight" -> "Direct Coaching"
- Update corresponding descriptions and icons to match new titles

**How It Works section:**
- `bg-zinc-50` -> `bg-card/50` (subtle contrast from background)
- Badge, h2, p: `text-zinc-500` -> `text-muted-foreground`. Add `font-display` to h2. Change h2 to "HOW IT WORKS"
- Step circles: `bg-emerald-600` -> `bg-primary`
- Step text: `text-zinc-500` -> `text-muted-foreground`
- Update steps per CONTEXT to 3 steps: Apply -> Get Your Plan -> Check In Weekly (remove step 4, renumber to 01/02/03)

Do NOT touch any useState, useEffect, useRef, useInView, motion components, event handlers, or the submitLead function. Only className strings and static text content change.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Landing page top half (hero through How It Works) renders with zinc-950 background, Bebas Neue headlines, emerald accents, dark cards. No white/light backgrounds visible. All framer-motion animations intact. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle landing page comparison table, results, pricing, FAQ, CTA, and footer</name>
  <files>src/app/page.tsx</files>
  <action>
Continue restyling the bottom half of page.tsx (from "DIY vs Coached" through footer and exit popup). Same rules: preserve ALL functionality, only change classNames and specified text.

**DIY vs Coached comparison table:**
- h2: Add `font-display`, change to "WHY COACHING WINS". `text-zinc-500` -> `text-muted-foreground`
- Table container: `border rounded-xl` -> `border border-border rounded-xl overflow-hidden`
- Header row: `bg-zinc-50` -> `bg-card`, `bg-zinc-100` -> `bg-muted`
- MaxHealth column header: `bg-emerald-600` -> `bg-primary`
- Row text: add `text-foreground` where needed
- Row borders: `border-t` -> `border-t border-border`
- X icons: `text-zinc-300` -> `text-muted-foreground/50`
- Check column bg: `bg-emerald-50` -> `bg-primary/5`
- Check icons: `text-emerald-600` -> `text-primary`

**Results section:**
- `bg-zinc-50` -> `bg-card/50`
- Badge, h2, p: apply `font-display` to h2, `text-muted-foreground` to descriptive text. Change h2 to "HUNDREDS TRANSFORMED"
- Stat cards: `bg-white border` -> `bg-card border border-border`
- Stat numbers: `text-emerald-600` -> `text-primary font-display`
- Stat labels: `text-zinc-500` -> `text-muted-foreground`
- Disclaimer: `text-zinc-400` -> `text-muted-foreground/70`
- CTA button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`

**Free Tools section:**
- Badge, h2, p: `text-zinc-500` -> `text-muted-foreground`. Add `font-display` to h2
- Tool cards: Replace `hover:shadow-md hover:border-emerald-200` with `hover-card-glow`
- Icon: `text-emerald-600` -> `text-primary`
- Text: `text-zinc-500` -> `text-muted-foreground`

**Pricing section (inline on landing page):**
- `bg-zinc-50` -> `bg-card/50`
- Badge, h2, p: `text-zinc-500` -> `text-muted-foreground`. Add `font-display` to h2
- Cards: popular card `border-emerald-500 border-2 shadow-xl` -> `border-primary border-2 shadow-xl shadow-primary/10`
- Price: `text-zinc-500` -> `text-muted-foreground`
- Feature check: `text-emerald-500` -> `text-primary`
- Feature text: `text-zinc-600` -> `text-muted-foreground`
- CTA buttons: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- Trust signals: `text-zinc-400` -> `text-muted-foreground/70`

**FAQ section:**
- Badge, h2: add `font-display` to h2
- Accordion items: `border rounded-lg` -> `border border-border rounded-lg bg-card`
- Answer text: `text-zinc-500` -> `text-muted-foreground`

**Final CTA section:**
- `bg-zinc-900 text-white` -> `bg-gradient-to-b from-card to-background` (already dark, just refine)
- h2: Add `font-display`, change to "READY TO TRANSFORM?"
- p: `text-zinc-400` -> `text-muted-foreground`
- Button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`

**Footer:**
- `border-t py-12 bg-white` -> `border-t border-border py-12 bg-card`
- Logo span: `text-emerald-600` -> `text-primary`
- Text: `text-zinc-500` -> `text-muted-foreground`
- Links hover: `hover:text-zinc-900` -> `hover:text-foreground`
- "Subscribed!" text: `text-emerald-600` -> `text-primary`
- Subscribe button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- Bottom border: `border-t` -> `border-t border-border`
- Disclaimer: `text-zinc-400` -> `text-muted-foreground/70`

**Mobile sticky bar:**
- `bg-white border-t` -> `bg-card border-t border-border`
- Button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`

**Exit intent popup:**
- Dialog is already dark-themed from Phase 7 component overrides
- Check icon: `text-emerald-600` -> `text-primary`
- Text: `text-zinc-500` -> `text-muted-foreground`
- Button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- Small text: `text-zinc-400` -> `text-muted-foreground/70`

Do NOT touch any state management, event handlers, or functional code.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Landing page bottom half fully dark-themed. Comparison table, results stats, pricing cards, FAQ accordion, final CTA, footer, mobile bar, and exit popup all use dark surfaces with emerald accents. No white/light backgrounds anywhere on the page. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` passes with no errors
- Grep for `bg-white`, `bg-gray-50`, `bg-zinc-50`, `bg-zinc-100` in src/app/page.tsx returns zero matches (all replaced with dark equivalents)
- Grep for `text-zinc-600`, `text-zinc-500` used as standalone (not inside a dark semantic) returns zero or is replaced with text-muted-foreground
- All framer-motion imports and usage preserved
- submitLead function unchanged
- exit intent popup functionality unchanged
</verification>

<success_criteria>
- Landing page renders entirely on dark backgrounds (zinc-950 base)
- Bebas Neue font applied to all section headings via font-display class
- Emerald-500 used consistently for CTAs, icons, and accents via bg-primary/text-primary
- All existing animations (scroll-triggered, hover, exit intent) work unchanged
- No hardcoded light color classes remain in page.tsx
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-pages/08-01-SUMMARY.md`
</output>
