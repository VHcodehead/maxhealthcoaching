---
phase: 08-public-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/login/page.tsx
  - src/app/signup/page.tsx
  - src/app/pricing/page.tsx
  - src/app/pending/page.tsx
autonomous: false
requirements: [PUB-02, PUB-03, PUB-04]

must_haves:
  truths:
    - "Login page shows a dark zinc-900 card on zinc-950 background with emerald sign-in button and no white panels"
    - "Apply page shows dark card with APPLY FOR COACHING Bebas Neue header, dark form sections, and emerald accents"
    - "Pricing page shows 3 dark zinc-900 cards with emerald highlights, Pro card has emerald border glow, trust signals below"
    - "Pending page is full-screen dark with emerald pulse clock animation and Bebas Neue APPLICATION UNDER REVIEW heading"
    - "All forms (login, apply) still submit correctly — no functionality broken"
    - "Pricing checkout flow still works — handleCheckout unchanged"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Dark-themed login page"
      contains: "bg-background"
    - path: "src/app/signup/page.tsx"
      provides: "Dark-themed apply page"
      contains: "APPLY FOR COACHING"
    - path: "src/app/pricing/page.tsx"
      provides: "Dark-themed pricing page"
      contains: "bg-background"
    - path: "src/app/pending/page.tsx"
      provides: "Dark-themed pending page"
      contains: "APPLICATION UNDER REVIEW"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "next-auth signIn"
      via: "signIn('credentials', ...)"
      pattern: "signIn"
    - from: "src/app/signup/page.tsx"
      to: "/api/auth/signup"
      via: "fetch POST"
      pattern: "fetch.*api/auth/signup"
    - from: "src/app/pricing/page.tsx"
      to: "/api/checkout"
      via: "fetch POST in handleCheckout"
      pattern: "fetch.*api/checkout"
---

<objective>
Restyle the 4 auth/utility public pages (login, apply/signup, pricing, pending) from white/light theme to dark athletic hybrid design.

Purpose: These pages complete the prospect journey. After seeing the dark landing page, visitors must not encounter a jarring white form page when they click "Apply Now" or "Sign In".
Output: All 4 pages dark-themed with emerald accents, Bebas Neue headings, consistent with the landing page.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-public-pages/08-CONTEXT.md
@.planning/phases/07-design-foundation/07-01-SUMMARY.md
@.planning/phases/07-design-foundation/07-02-SUMMARY.md
@src/app/globals.css
@src/app/login/page.tsx
@src/app/signup/page.tsx
@src/app/pricing/page.tsx
@src/app/pending/page.tsx

<interfaces>
<!-- Phase 7 CSS variables available (from globals.css :root) -->
- bg-background = zinc-950
- bg-card = zinc-900
- bg-primary / text-primary = emerald-500
- text-foreground = white
- text-muted-foreground = zinc-400ish
- border-border = white/10
- bg-input = white/5
- ring-ring = emerald-500

<!-- Phase 7 utility classes -->
- .transition-micro — 200ms ease
- .hover-card-glow — scale + emerald border glow
- font-display — Bebas Neue for headings

<!-- shadcn components already dark-themed from Phase 7 -->
- Card, Button, Input, Badge, Dialog — all use dark CSS vars
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle login and pending pages to dark theme</name>
  <files>src/app/login/page.tsx, src/app/pending/page.tsx</files>
  <action>
Restyle both pages. Preserve ALL form logic, hooks, signIn calls, router navigation, search params, Suspense boundaries. Only change className strings and specified text.

**LOGIN PAGE (src/app/login/page.tsx):**

Container and layout:
- `bg-gray-50` -> `bg-background` (both in LoginContent and Suspense fallback spinner)
- Spinner: `border-zinc-200 border-t-zinc-600` -> `border-border border-t-primary`

Logo area:
- Dumbbell container: `bg-emerald-600` -> `bg-primary` and add subtle glow: `shadow-lg shadow-primary/20`
- h1 "MaxHealth Coaching": add `font-display text-3xl` (was text-2xl font-bold), change to "MAX<span class='text-primary'>HEALTH</span>" pattern. Keep tracking-tight.
- Subtitle: keep `text-muted-foreground` (already uses it)

Card (already dark from Phase 7 but verify):
- CardTitle "Sign in to your account": keep or change to just "SIGN IN" with font-display
- Button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- "Apply Now" link at bottom: `text-foreground hover:underline` -> `text-primary hover:underline`

**PENDING PAGE (src/app/pending/page.tsx):**

Container:
- `bg-gray-50` -> `bg-background` (both in PendingContent and Suspense fallback)

Logo area: Same treatment as login page (Dumbbell container, h1 with font-display)

Card content:
- Remove the Card wrapper entirely. Instead, use a simple centered div layout directly on zinc-950 background for a more dramatic full-screen feel.
- Clock icon: `text-emerald-600` -> `text-primary animate-pulse` (add pulse animation per CONTEXT)
- XCircle (rejected): `text-red-500` -> `text-destructive`
- Title "Application Under Review": Change to "APPLICATION UNDER REVIEW" with `font-display text-2xl` class
- Title (rejected): Change to "APPLICATION NOT ACCEPTED" with font-display
- Description text: already uses CardDescription/muted-foreground, change to plain `text-muted-foreground` p tags since we're removing Card
- "Back to Login" link: `text-emerald-600` -> `text-primary`
- Add `text-muted-foreground/50` for the "Back to Login" link per CONTEXT (zinc-500 link)
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Login page renders dark card on zinc-950 with emerald glow logo and primary sign-in button. Pending page renders full-screen dark with pulsing emerald clock and Bebas Neue heading. Both pages have zero white/light backgrounds. Forms and navigation work unchanged. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle apply (signup) page to dark theme</name>
  <files>src/app/signup/page.tsx</files>
  <action>
Restyle the apply/signup page. Preserve ALL form logic, react-hook-form, zodResolver, API calls, state management. Only change className strings and specified text.

**Container:**
- `bg-gray-50` -> `bg-background`

**Logo area:** Same treatment as login (Dumbbell with primary + glow, font-display h1)

**Success state (submitted=true):**
- CheckCircle: `text-emerald-600` -> `text-primary`
- "Back to Login" link: `text-emerald-600` -> `text-primary`

**Card header:**
- CardTitle "Apply for Coaching": Change to "APPLY FOR COACHING" with `font-display text-2xl` class
- CardDescription: keep (uses muted-foreground)

**Form sections:**
- Section headers (h3 "Personal Info", "Your Goals", etc.): Add `text-primary` to make them emerald, keep uppercase tracking-wide. Change font-semibold to font-display for Bebas Neue.
- All select elements: They currently use `border-input bg-background` which will resolve to dark. Verify the className string uses semantic vars. Add `text-foreground` to ensure white text on dark selects. Also add `[&>option]:bg-card [&>option]:text-foreground` for dropdown options styling on dark.
- Textarea: Same pattern — uses `border-input bg-background`, should resolve dark. Verify `text-foreground` is present or inherited.

**Buttons:**
- Submit: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- "Sign in" link at bottom: already uses `text-foreground hover:underline`

**Labels:** Already use Label component which inherits foreground color. No changes needed.
**Inputs:** Already use Input component which is dark from Phase 7. No changes needed.
**Error messages:** Use `text-destructive` which is already set. No changes needed.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Apply page renders dark with Bebas Neue "APPLY FOR COACHING" header, emerald section headers, dark form inputs and selects, primary submit button. All form validation and API submission work unchanged. No white backgrounds. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle pricing page to dark theme</name>
  <files>src/app/pricing/page.tsx</files>
  <action>
Restyle the pricing page. Preserve ALL checkout logic, useSession, handleCheckout, router navigation, loading states. Only change className strings and specified text.

**Root container:**
- `min-h-screen bg-white` -> `min-h-screen bg-background`

**Header bar:**
- `border-b` -> `border-b border-border bg-background`
- Logo: `text-emerald-600` -> `text-primary`
- "Sign in" link: `text-zinc-500 hover:text-zinc-900` -> `text-muted-foreground hover:text-foreground`

**Page header:**
- Badge: keep (already dark-themed)
- h1: Add `font-display` class. Change "Invest in Your Transformation" to "INVEST IN YOUR TRANSFORMATION"
- Description p: `text-zinc-500` -> `text-muted-foreground`

**Pricing cards:**
- Non-popular cards: `border-zinc-200` -> `border-border`
- Popular (Pro) card: `border-emerald-500 border-2 shadow-xl shadow-emerald-500/10 scale-105` -> `border-primary border-2 shadow-xl shadow-primary/20 scale-105` (slightly stronger glow per CONTEXT)
- Card title: add `font-display text-xl` (was text-lg font-medium)
- Description: `text-zinc-500` -> `text-muted-foreground`
- Price number: Add `font-display text-5xl` (was text-4xl font-bold) per CONTEXT (Bebas Neue prices)
- Price period: `text-zinc-500` -> `text-muted-foreground`
- Feature check icon: `text-emerald-500` -> `text-primary`
- Feature text: `text-zinc-600` -> `text-muted-foreground`
- Popular button: `bg-emerald-600 hover:bg-emerald-700` -> `bg-primary hover:bg-primary/90`
- Non-popular buttons: Use variant="outline" which is already emerald-themed from Phase 7

**Trust signals:**
- Container: `text-zinc-500` -> `text-muted-foreground/70`
- Per CONTEXT, restructure to 3 trust signals with icons:
  - Shield + "Secure payment" (already has Shield icon)
  - Add Calendar icon import + "Cancel anytime"
  - Add ShieldCheck or Shield icon + "30-day guarantee"
- Style: `text-muted-foreground` with icon `text-muted-foreground/50`

**FAQ teaser:**
- h3: `text-xl font-semibold` -> add `font-display text-2xl`
- p: `text-zinc-500` -> `text-muted-foreground`
- Links: `text-emerald-600` -> `text-primary`

**Footer/disclaimer:**
- `border-t py-8 mt-16` -> `border-t border-border py-8 mt-16`
- `text-zinc-400` -> `text-muted-foreground/70`

Add `Calendar` to the lucide-react import statement for the trust signals.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Pricing page renders 3 dark cards on zinc-950 background. Pro card has emerald border glow. Prices in Bebas Neue. Trust signals with icons below cards. All checkout functionality works unchanged. No white backgrounds. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` passes with no errors
- Grep for `bg-gray-50` in all 4 files returns zero matches
- Grep for `bg-white` in all 4 files returns zero matches
- Login form submits and redirects correctly
- Apply form submits with validation
- Pricing checkout redirects to Stripe
- Pending page shows correct state based on ?status param
</verification>

<success_criteria>
- All 4 pages render on zinc-950 backgrounds with no light artifacts
- Bebas Neue applied to main headings via font-display class
- Emerald-500 used consistently for CTAs and accents via semantic vars
- All forms, API calls, redirects, and session checks work unchanged
- No hardcoded light color classes remain in any of the 4 files
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-pages/08-02-SUMMARY.md`
</output>
