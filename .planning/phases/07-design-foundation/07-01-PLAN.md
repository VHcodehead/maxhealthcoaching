---
phase: 07-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - THEME-01
  - THEME-02

must_haves:
  truths:
    - "The site background is zinc-950 globally when no page overrides are present"
    - "Emerald-500 is the primary accent color in the CSS variable system"
    - "Bebas Neue font is loaded and available via font-display CSS variable"
    - "Inter remains the body font via font-sans CSS variable"
    - "No .dark class variant exists — dark is the only mode"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Dark-by-default CSS variable system with emerald primary"
      contains: "--background"
    - path: "src/app/layout.tsx"
      provides: "Bebas Neue + Inter font loading"
      contains: "Bebas_Neue"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/ui/*.tsx"
      via: "CSS variables consumed by Tailwind semantic classes"
      pattern: "bg-background|bg-card|bg-primary|text-foreground"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variables --font-sans and --font-display"
      pattern: "--font-display|--font-sans"
---

<objective>
Rewrite the CSS variable system to be dark-by-default with the Dark Athletic Hybrid palette, and add Bebas Neue as the display font alongside Inter.

Purpose: This is the foundation that every shadcn component and page consumes. Changing `:root` vars propagates the dark palette to every component using semantic classes (`bg-background`, `bg-card`, `text-foreground`, etc.) without touching individual component files.

Output: Dark zinc-950 site background, emerald-500 primary accent, Bebas Neue display font loaded and mapped to `--font-display`.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-foundation/07-CONTEXT.md

@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CSS variables to dark-by-default with emerald primary</name>
  <files>src/app/globals.css</files>
  <action>
Rewrite `src/app/globals.css` with these changes:

1. **Remove the `.dark` class block entirely** (lines 85-117). Dark IS the only mode.

2. **Remove the `@custom-variant dark` line** (line 5). No dark variant needed.

3. **Rewrite `:root` variables** to the Dark Athletic Hybrid palette. Convert hex targets to oklch:
   - `--background`: zinc-950 (#09090b) — approximately `oklch(0.141 0.005 285.823)`
   - `--foreground`: zinc-50 (#fafafa) — `oklch(0.985 0 0)`
   - `--card`: zinc-900 (#18181b) — approximately `oklch(0.21 0.006 285.885)`
   - `--card-foreground`: zinc-50 (#fafafa) — `oklch(0.985 0 0)`
   - `--popover`: zinc-800 (#27272a) — approximately `oklch(0.274 0.006 286.033)`
   - `--popover-foreground`: zinc-50 (#fafafa) — `oklch(0.985 0 0)`
   - `--primary`: emerald-500 (#10b981) — approximately `oklch(0.696 0.17 162.48)`
   - `--primary-foreground`: white (#ffffff) — `oklch(1 0 0)`
   - `--secondary`: zinc-800 (#27272a) — approximately `oklch(0.274 0.006 286.033)`
   - `--secondary-foreground`: zinc-50 (#fafafa) — `oklch(0.985 0 0)`
   - `--muted`: zinc-800 (#27272a) — approximately `oklch(0.274 0.006 286.033)`
   - `--muted-foreground`: zinc-400 (#a1a1aa) — approximately `oklch(0.708 0.015 286.067)`
   - `--accent`: zinc-800 (#27272a) — approximately `oklch(0.274 0.006 286.033)`
   - `--accent-foreground`: zinc-50 (#fafafa) — `oklch(0.985 0 0)`
   - `--destructive`: red-500 (#ef4444) — approximately `oklch(0.637 0.237 25.331)`
   - `--border`: `oklch(1 0 0 / 10%)` (white at 10% opacity)
   - `--input`: `oklch(1 0 0 / 5%)` with border at `oklch(1 0 0 / 10%)`
   - `--ring`: emerald-500 — same as `--primary`
   - `--sidebar`: zinc-900 — same as `--card`
   - `--sidebar-foreground`: zinc-50 — same as `--foreground`
   - `--sidebar-primary`: emerald-500 — same as `--primary`
   - `--sidebar-primary-foreground`: white — same as `--primary-foreground`
   - `--sidebar-accent`: zinc-800 — same as `--accent`
   - `--sidebar-accent-foreground`: zinc-50 — same as `--accent-foreground`
   - `--sidebar-border`: `oklch(1 0 0 / 10%)` — same as `--border`
   - `--sidebar-ring`: emerald-500 — same as `--ring`

4. **Update chart colors** for dark background visibility:
   - `--chart-1`: emerald-500 (primary brand)
   - `--chart-2`: blue-400 (protein macro color — brighten for dark bg)
   - `--chart-3`: amber-400 (carbs macro color — brighten for dark bg)
   - `--chart-4`: rose-400 (fat macro color — brighten for dark bg)
   - `--chart-5`: purple-400

5. **Add `--font-display` to the `@theme inline` block:**
   ```
   --font-display: var(--font-display);
   ```

6. **Keep** the `--radius: 0.625rem` value unchanged.

7. **Keep** the `@layer base` block unchanged (it uses semantic classes which will pick up new vars).

Note: Use the oklch values from the existing `.dark` block as reference where they match the target colors. For emerald-500, the oklch value `oklch(0.696 0.17 162.48)` is already present as chart-2 in the old dark block.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>`:root` contains dark palette with zinc-950 background and emerald-500 primary. `.dark` class block is removed. `@custom-variant dark` line is removed. `--font-display` mapped in @theme inline block. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bebas Neue display font alongside Inter</name>
  <files>src/app/layout.tsx</files>
  <action>
Modify `src/app/layout.tsx` to load Bebas Neue from Google Fonts:

1. **Import Bebas_Neue** from `next/font/google` alongside the existing Inter import:
   ```typescript
   import { Inter, Bebas_Neue } from 'next/font/google';
   ```

2. **Initialize Bebas Neue** with weight 400 (it only has one weight), latin subset, and the `--font-display` CSS variable:
   ```typescript
   const bebasNeue = Bebas_Neue({
     weight: '400',
     subsets: ['latin'],
     variable: '--font-display',
   });
   ```

3. **Add the Bebas Neue variable to the body className**:
   ```tsx
   <body className={`${inter.variable} ${bebasNeue.variable} font-sans antialiased`}>
   ```

4. **Do NOT change anything else** in layout.tsx — metadata, Providers, Toaster all stay.

The `font-display` utility class will now be available via Tailwind (mapped through `@theme inline` in globals.css). Usage: `font-display` class on any element to apply Bebas Neue.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Bebas Neue loads via next/font/google with `--font-display` CSS variable. Body element has both `inter.variable` and `bebasNeue.variable` classes. Build succeeds with no font-related errors.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. CSS variables in `:root` define dark palette (no light defaults)
3. No `.dark` class block exists in globals.css
4. `--font-display` is mapped in both `@theme inline` and set by Bebas Neue font loader
5. Both `--font-sans` (Inter) and `--font-display` (Bebas Neue) CSS variables are present on the body element
</verification>

<success_criteria>
- Build passes with zero errors
- `:root` background is zinc-950 (dark), not white
- Primary accent is emerald-500, not gray/neutral
- Bebas Neue font file is loaded (visible in page source/network tab)
- `font-display` Tailwind class applies Bebas Neue
- No `.dark` class variant remains in globals.css
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-foundation/07-01-SUMMARY.md`
</output>
