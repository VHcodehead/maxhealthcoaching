---
phase: 03-coach-editing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/app/coach/clients/[id]/page.tsx
  - src/components/coach/edit-training-day-dialog.tsx
autonomous: false
requirements:
  - EDIT-02
  - EDIT-03
  - EDIT-04

must_haves:
  truths:
    - "Coach can add a new exercise in the training day dialog and it persists after save"
    - "Coach can remove an exercise in the training day dialog and it persists after save"
    - "Coach can toggle cardio on/off, set type and duration, and changes persist after save"
    - "Client dashboard meals page shows meals added/edited/removed by coach"
    - "Client dashboard training page shows exercises and cardio changes made by coach"
    - "All generated plan content (macros, meals, training, supplements) is editable by coach"
  artifacts:
    - path: "src/components/coach/edit-training-day-dialog.tsx"
      provides: "Exercise add/remove and cardio editing"
      contains: "addExercise"
    - path: "src/app/coach/clients/[id]/page.tsx"
      provides: "All editing controls wired and functional"
  key_links:
    - from: "src/components/coach/edit-training-day-dialog.tsx"
      to: "src/app/coach/clients/[id]/page.tsx"
      via: "onSave callback propagates to PUT /api/admin/training-plan"
      pattern: "handleSaveExerciseEdit"
    - from: "src/app/coach/clients/[id]/page.tsx"
      to: "/api/admin/training-plan"
      via: "fetch PUT on training day save"
      pattern: "fetch.*api/admin/training-plan"
---

<objective>
Verify and fix exercise add/remove, cardio editing, and complete the full editability audit across all plan content types.

Purpose: EDIT-02, EDIT-03, and EDIT-04 require verifying that existing editing features work end-to-end and that all generated content is fully editable by the coach. This is verification-and-fix work, not greenfield development.
Output: All coach editing capabilities verified working with any bugs fixed. Human verification of the complete editing experience.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-coach-editing/03-CONTEXT.md
@.planning/phases/03-coach-editing/03-01-SUMMARY.md

@src/app/coach/clients/[id]/page.tsx
@src/components/coach/edit-training-day-dialog.tsx
@src/components/coach/edit-meal-dialog.tsx
@src/components/coach/macro-override-form.tsx
@src/components/coach/coach-supplements.tsx
@src/app/api/admin/training-plan/route.ts
@src/app/api/admin/meal-plan/route.ts
@src/app/dashboard/meals/page.tsx
@src/app/dashboard/training/page.tsx

<interfaces>
From src/components/coach/edit-training-day-dialog.tsx:
```typescript
interface EditTrainingDayDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  day: TrainingDay;
  weekIndex: number;
  dayIndex: number;
  onSave: (weekIndex: number, dayIndex: number, updatedDay: TrainingDay) => void;
}
// Already has: addExercise(), removeExercise(index)
// Already has: cardioEnabled toggle, cardioType, cardioDuration state
```

From src/types/database.ts:
```typescript
export interface TrainingDay {
  day_name?: string;
  day?: string;
  name?: string;
  session_name?: string;
  muscle_groups?: string[];
  exercises: Exercise[];
  warmup?: string[];
  cooldown?: string;
  notes?: string;
  cardio?: { type: string; duration_minutes: number; };
}

export interface Exercise {
  name: string;
  sets: number;
  reps: string;
  rest_seconds: number;
  notes?: string;
  substitution?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix exercise add/remove and cardio editing end-to-end</name>
  <files>src/components/coach/edit-training-day-dialog.tsx, src/app/coach/clients/[id]/page.tsx</files>
  <action>
This task is verification-first: test the existing functionality, then fix any bugs found.

**EDIT-02: Exercise Add/Remove Verification**

1. Read `edit-training-day-dialog.tsx` fully. Verify:
   - `addExercise()` creates a valid Exercise object with all required fields (name, sets, reps, rest_seconds)
   - `removeExercise(index)` correctly splices the exercise from the array
   - The `handleSave` function includes exercises in the updatedDay object passed to `onSave`
   - The dialog renders add/remove buttons that are accessible and functional

2. Read the training plan rendering section in `page.tsx` (around the "Training Plan" card). Verify:
   - Each training day has an edit button that opens EditTrainingDayDialog
   - The `handleSaveExerciseEdit` function correctly saves via PUT `/api/admin/training-plan`
   - The local state updates after save

3. Fix any issues found. Common bugs to check:
   - Exercise fields might not all be editable in the dialog (check each field has an input)
   - The `onSave` callback might not pass the cardio field
   - The save handler might not handle the case where exercises array is empty

**EDIT-03: Cardio Editing Verification**

1. In `edit-training-day-dialog.tsx`, verify:
   - `cardioEnabled` toggle works (checkbox or switch)
   - When cardio is enabled, type and duration fields are editable
   - When cardio is disabled, the cardio field is set to `undefined` or `null` in the saved day
   - The `handleSave` function includes/excludes cardio correctly based on toggle state

2. Check that `handleSave` in the dialog builds the updatedDay correctly:
   - If cardioEnabled: `cardio: { type: cardioType, duration_minutes: cardioDuration }`
   - If not cardioEnabled: `cardio: undefined`

3. Fix any issues. Common bug: cardio might be included even when toggled off, or duration might not be a number.

**EDIT-04: Full Editability Audit**

Verify each content type is editable from the coach client detail page:

1. **Macros** — `macro-override-form.tsx`: Read the component, verify it saves via API and updates state. Already confirmed working per CONTEXT.md, but verify the form is rendered in the page.
2. **Meals** — Handled by Plan 01 (add/remove/edit). Verify the EditMealDialog is wired correctly for editing existing meals.
3. **Training** — Verified above (exercises + cardio).
4. **Supplements** — `coach-supplements.tsx`: Read the component, verify it renders and saves. Already confirmed working per CONTEXT.md, but verify it's rendered in the page.

5. Check client dashboard display:
   - Read `src/app/dashboard/meals/page.tsx` — verify it reads from the same meal plan data that the coach edits
   - Read `src/app/dashboard/training/page.tsx` — verify it reads from the same training plan data
   - If either page uses hardcoded data or different endpoints, fix to use the same source

Fix any bugs found during the audit. If no bugs are found, document the verification results.
  </action>
  <verify>
    <automated>cd C:/training-website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Exercise add/remove works in the dialog and persists after save
- Cardio toggle, type, and duration editing works and persists
- All four content types (macros, meals, training, supplements) are editable from coach page
- Client dashboard pages display the same data the coach edits
- TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete coach editing experience</name>
  <files>src/app/coach/clients/[id]/page.tsx</files>
  <action>
Present the complete editing experience for human verification. All automated work is complete — this checkpoint confirms everything works visually and functionally from the coach and client perspectives.
  </action>
  <verify>
    <automated>echo "Checkpoint: human verification required"</automated>
  </verify>
  <done>Human confirms all editing operations work correctly</done>
  <what-built>Complete coach editing experience: add/remove meals (Plan 01), exercise add/remove, cardio editing, and full editability across macros, meals, training, and supplements.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in as coach, navigate to a client detail page that has a generated meal plan and training plan
    3. **Meal Add**: Expand Meal Plan section. Click "Add Meal" on any day. Fill in the blank meal dialog and save. Verify the new meal appears in the day and day totals updated.
    4. **Meal Delete**: Click the trash icon on any meal. Confirm deletion. Verify the meal is removed and day totals updated.
    5. **Meal Edit**: Click the pencil icon on any meal. Change a field and save. Verify changes appear.
    6. **Refresh**: Refresh the page. Verify all meal changes persisted.
    7. **Exercise Add/Remove**: Expand Training Plan. Click edit on a training day. Add a new exercise, save. Re-open and remove an exercise, save. Verify changes persist after refresh.
    8. **Cardio**: Edit a training day. Toggle cardio on, set type and duration, save. Verify cardio appears. Toggle cardio off, save. Verify cardio removed.
    9. **Client View**: Log in as that client (or check client dashboard pages). Verify meal and training changes are visible on the client's meals and training pages.
    10. **Macros + Supplements**: Verify macro override form and supplement editor are present and functional on the coach page.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. All editing operations work end-to-end (add, edit, remove for meals, exercises, cardio)
3. Changes persist after page refresh
4. Client dashboard reflects coach edits
5. Human verification confirms the complete editing experience
</verification>

<success_criteria>
- All four EDIT requirements (EDIT-01 through EDIT-04) are satisfied
- Coach can fully manage every aspect of a client's plan without regenerating
- Client sees all coach edits on their dashboard
- Human approves the complete editing experience
</success_criteria>

<output>
After completion, create `.planning/phases/03-coach-editing/03-02-SUMMARY.md`
</output>
