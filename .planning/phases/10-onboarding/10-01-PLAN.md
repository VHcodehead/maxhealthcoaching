---
phase: 10-onboarding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/onboarding/page.tsx
autonomous: true
requirements:
  - ONBOARD-01

must_haves:
  truths:
    - "The onboarding page has a zinc-950 background with no white or light-gray surfaces visible"
    - "The step progress bar shows a zinc-800 track with emerald-500 fill"
    - "SelectableCard selected state uses emerald-500 ring with emerald-950/30 background (not emerald-50)"
    - "TagInput badges use emerald-900/30 bg with emerald-400 text (not secondary variant)"
    - "Step titles use font-display (Bebas Neue) class"
    - "Navigation buttons: emerald-500 primary Next, zinc-800 Back"
    - "All form logic, step navigation, validation, and API calls work identically to before"
  artifacts:
    - path: "src/app/onboarding/page.tsx"
      provides: "Dark-themed 9-step onboarding form"
      contains: "bg-background"
  key_links:
    - from: "src/app/onboarding/page.tsx"
      to: "/api/onboarding"
      via: "fetch POST on submit"
      pattern: "fetch.*api/onboarding"
    - from: "src/app/onboarding/page.tsx"
      to: "/generating"
      via: "router.push on success"
      pattern: "router\\.push.*generating"
---

<objective>
Dark restyle the multi-step onboarding form (9 steps, ~723 lines) to the dark athletic hybrid theme.

Purpose: ONBOARD-01 — the onboarding flow is the first experience for paying clients after checkout. It must match the premium dark brand established in phases 7-9.
Output: Fully dark-themed onboarding page with all form logic preserved.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding/10-CONTEXT.md

@src/app/onboarding/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark restyle onboarding header, progress bar, and inline components</name>
  <files>src/app/onboarding/page.tsx</files>
  <action>
Restyle the onboarding page header, progress indicator, and the two inline components (SelectableCard, TagInput). This is a CSS-class-only change — do NOT modify any form logic, state, validation, or API calls.

**Page wrapper and header:**
- Change outer div from `bg-zinc-50` to `bg-background` (zinc-950)
- Change header from `bg-white border-b` to `bg-background border-b border-white/10`
- Change "MaxHealth" brand span: `text-emerald-600` to `text-primary` (emerald-500)
- Change step counter from `text-zinc-400` to `text-muted-foreground`
- Change step title from `text-zinc-600` to `text-zinc-400`
- Add `font-display` class to the step title text for Bebas Neue typography
- Progress bar: add `className="h-1.5 [&>div]:bg-primary"` if needed (the track should be zinc-800, fill emerald-500 — check if Phase 7 shadcn Progress component already handles this on dark, if so leave as-is)

**SelectableCard component (line ~45-56):**
- Default state: change `hover:border-zinc-300` to `hover:border-white/20`
- Selected state: change `bg-emerald-50 border-emerald-300` to `bg-emerald-950/30 border-transparent` (keep `ring-2 ring-emerald-500`)
- The Card base component should already have dark bg from Phase 7 overrides — if not, add `bg-zinc-900 border-white/10`

**TagInput component (line ~58-84):**
- Change Badge from `variant="secondary"` to explicit dark styling: `className="bg-emerald-900/30 text-emerald-400 hover:bg-emerald-900/50 cursor-pointer"`
- The Input and hint text should already be dark from Phase 7 — verify `text-zinc-400` on the "Press Enter to add" hint

**Navigation buttons (goNext/goBack at bottom of each step):**
- "Next" / "Continue" button: ensure it uses `bg-primary hover:bg-primary/90` (emerald-500)
- "Back" button: change to `variant="outline"` with explicit `bg-zinc-800 border-white/10 hover:bg-zinc-700 text-zinc-300`
- Final "Submit" button: same emerald primary pattern as Next

**Step headings (h2 elements like "Tell us about yourself", "What's your goal?", etc.):**
- Add `text-foreground` to ensure white text on dark background (the default `text-2xl font-bold` may already inherit, but be explicit)

**Goal icon backgrounds (step 2, line ~285):**
- Unselected: change `bg-zinc-100` to `bg-zinc-800`
- Selected: keep `bg-emerald-500 text-white`

**Body fat cards (step 4, line ~364):**
- Change `bg-emerald-50` to `bg-emerald-950/30`
- Change `hover:border-zinc-300` to `hover:border-white/20`

**Slider value displays (meals_per_day, time_per_session):**
- Change `text-emerald-600` to `text-primary` (emerald-500)

**Plan duration cards (step 9, line ~667):**
- Unselected icon bg: change `bg-zinc-100` to `bg-zinc-800`
- Selected: keep `bg-emerald-500 text-white`
- Badge: change `bg-emerald-100 text-emerald-700 hover:bg-emerald-100` to `bg-emerald-900/30 text-emerald-400 hover:bg-emerald-900/30`

**All Label elements:** Should already use `text-muted-foreground` from Phase 7 — verify they are not hardcoded to dark text colors.

**All hint text (text-xs text-zinc-400):** Keep as-is — zinc-400 works on dark backgrounds.

CRITICAL: Do NOT change any useState, useCallback, useRouter, fetch calls, form validation (canProceed), slideVariants, or any behavioral JavaScript. This is purely a className restyle.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Onboarding page builds without errors
- No bg-white, bg-zinc-50, bg-emerald-50, bg-zinc-100, text-emerald-600, text-emerald-700, border-zinc-300 classes remain (except where semantically correct for dark theme)
- All 9 steps render with dark surfaces, emerald accents, and Bebas Neue step titles
- Form logic completely unchanged
  </done>
</task>

</tasks>

<verification>
- `npx next build` passes with no errors
- Grep for light-mode classes: `grep -n "bg-white\|bg-zinc-50\|bg-emerald-50\|bg-emerald-100\|bg-zinc-100" src/app/onboarding/page.tsx` returns no matches
- All form state, validation, step navigation, and API submission logic unchanged
</verification>

<success_criteria>
The onboarding multi-step form is fully dark-themed: zinc-950 background, emerald-500 accents, Bebas Neue step titles, dark SelectableCards, dark TagInput badges. No light-mode artifacts remain. All 9 steps function identically.
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding/10-01-SUMMARY.md`
</output>
