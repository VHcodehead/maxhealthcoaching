---
phase: 10-onboarding
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/generating/page.tsx
  - src/app/checkin/page.tsx
  - src/app/success/page.tsx
autonomous: true
requirements:
  - ONBOARD-02

must_haves:
  truths:
    - "The generating page has a zinc-950 background with no white or light surfaces"
    - "The generating page heading uses font-display (Bebas Neue) for 'BUILDING YOUR CUSTOM PLANS'"
    - "Step cards on generating page use dark surfaces with emerald accents for active/complete states"
    - "The check-in page has a zinc-950 background with dark header and dark form cards"
    - "The success page has a zinc-950 background with dark card and emerald accent icon"
    - "All API calls, routing, form logic, and animations work identically to before"
  artifacts:
    - path: "src/app/generating/page.tsx"
      provides: "Dark-themed plan generation loading page"
      contains: "bg-background"
    - path: "src/app/checkin/page.tsx"
      provides: "Dark-themed weekly check-in form"
      contains: "bg-background"
    - path: "src/app/success/page.tsx"
      provides: "Dark-themed post-checkout success page"
      contains: "bg-background"
  key_links:
    - from: "src/app/generating/page.tsx"
      to: "/api/macros, /api/meal-plan, /api/training-plan"
      via: "sequential fetch POST calls"
      pattern: "fetch.*endpoint.*POST"
    - from: "src/app/checkin/page.tsx"
      to: "/api/checkin"
      via: "fetch GET/POST"
      pattern: "fetch.*api/checkin"
    - from: "src/app/success/page.tsx"
      to: "/api/checkout/verify"
      via: "fetch POST"
      pattern: "fetch.*api/checkout/verify"
---

<objective>
Dark restyle the generating/loading page, check-in page, and success page — the final three light-colored pages in the app.

Purpose: ONBOARD-02 (generating page) plus bonus completion of the last remaining light pages (checkin, success) to achieve 100% dark theme coverage across the entire application.
Output: Three fully dark-themed pages with all logic preserved.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding/10-CONTEXT.md

@src/app/generating/page.tsx
@src/app/checkin/page.tsx
@src/app/success/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark restyle generating page with premium loading aesthetic</name>
  <files>src/app/generating/page.tsx</files>
  <action>
Restyle the plan generating/loading page to the dark athletic hybrid theme. This is a CSS-class-only change plus upgrading the heading typography. Do NOT modify any fetch logic, step sequencing, router.push, or error handling.

**Page wrapper (line 75):**
- Change `bg-white` to `bg-background`

**Heading section:**
- Change h1 to include `font-display uppercase tracking-wider` for Bebas Neue: "BUILDING YOUR CUSTOM PLANS" / "YOUR PLANS ARE READY!"
- Make it larger: `text-4xl` instead of `text-3xl`
- Ensure text is white (add `text-foreground` if needed)
- Change subtext from `text-zinc-500` to `text-zinc-400`

**Step cards (the 3 generation steps):**
- Pending state: change `border-zinc-100 bg-zinc-50` to `border-white/10 bg-zinc-900`
- Loading state: change `border-emerald-200 bg-emerald-50` to `border-emerald-500/30 bg-emerald-950/20`
- Complete state: change `border-emerald-300 bg-emerald-50` to `border-emerald-500/30 bg-emerald-950/20`
- Error state: change `border-red-200 bg-red-50` to `border-red-500/30 bg-red-950/20`

**Step icon circles:**
- Pending: change `bg-zinc-100` to `bg-zinc-800`
- Loading: change `bg-emerald-100` to `bg-emerald-900/50`; change icon `text-emerald-600` to `text-emerald-400`
- Complete: keep `bg-emerald-500` with white icon
- Error: change `bg-red-100` to `bg-red-900/50`

**Step label text:**
- Pending: keep `text-zinc-400`
- Loading: change `text-emerald-700` to `text-emerald-400`
- Complete: change `text-emerald-700` to `text-emerald-400`
- Error: change `text-red-700` to `text-red-400`
- Loading description: change `text-emerald-600` to `text-emerald-400/70`

**Error box (line 174-188):**
- Change `bg-red-50 border border-red-200` to `bg-red-950/20 border border-red-500/30`
- Change `text-red-700` to `text-red-400`
- Change retry link `text-red-600` to `text-red-400`

**Completion spinner (line 196):**
- Change `border-emerald-200 border-t-emerald-600` to `border-emerald-900 border-t-emerald-400`

CRITICAL: Do NOT change any useEffect, fetch calls, step status logic, router.push, or setTimeout behavior.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Generating page builds without errors
- No bg-white, bg-zinc-50, bg-emerald-50, bg-emerald-100, bg-zinc-100, bg-red-50 classes remain
- Heading shows in Bebas Neue (font-display), uppercase
- All 3 generation steps display with dark cards and emerald accents
- Step sequencing, API calls, and redirect logic unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Dark restyle check-in page and success page</name>
  <files>src/app/checkin/page.tsx, src/app/success/page.tsx</files>
  <action>
Restyle both remaining light-colored pages to match the dark athletic hybrid theme. CSS-class changes only — preserve ALL form logic, react-hook-form bindings, photo upload, and API calls.

**CHECK-IN PAGE (src/app/checkin/page.tsx):**

Gate screens (loading, window-closed, already-done, done states — lines 199-297):
- All `bg-zinc-50` wrappers → `bg-background`
- `text-zinc-700` in countdown → `text-zinc-300`
- All emerald buttons already use `bg-emerald-600` — keep as-is (or update to `bg-primary hover:bg-primary/90` for consistency)

Main form (line 300+):
- Outer div: `bg-zinc-50` → `bg-background`
- Header: `bg-white border-b` → `bg-background border-b border-white/10`
- Header title: ensure `text-foreground`
- Header subtitle `text-zinc-500` → `text-zinc-400`
- All Card components should already have dark bg from Phase 7 — verify they render as zinc-900. If any Card has explicit light classes, remove them.
- CardTitle icon colors: keep `text-emerald-600` or update to `text-primary`
- Slider value `text-emerald-600` → `text-primary`
- Textarea (line ~448): change `border-zinc-200` to `border-white/10`, add `bg-zinc-900 text-foreground` 
- Photo upload border: change `border-zinc-200 hover:border-zinc-300` to `border-white/10 hover:border-white/20`
- Photo upload empty state `text-zinc-400` — keep as-is
- "Skip Photos" outline button: add `border-white/10 hover:bg-zinc-800`

**SUCCESS PAGE (src/app/success/page.tsx):**

- Outer div (line 46): `bg-white` → `bg-background`
- Suspense fallback (line 121): `bg-white` → `bg-background`
- Icon circle: `bg-emerald-100` → `bg-emerald-900/50`; icon `text-emerald-600` → `text-emerald-400`
- h1: add `text-foreground` if needed
- Paragraph `text-zinc-500` → `text-zinc-400`
- "What happens next" box: `bg-zinc-50` → `bg-zinc-900`
- List text `text-zinc-600` → `text-zinc-400`
- Step numbers `text-emerald-600` → `text-emerald-400`
- Heading icon `text-emerald-600` → `text-emerald-400`
- Fallback link `text-emerald-600` → `text-emerald-400`
- Buttons: already use `bg-emerald-600 hover:bg-emerald-700` — update to `bg-primary hover:bg-primary/90` for consistency

CRITICAL: Do NOT change any react-hook-form bindings, zodResolver, useEffect status checks, photo upload logic, fetch calls, or router navigation.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- Both pages build without errors
- No bg-white, bg-zinc-50, bg-emerald-50, bg-zinc-100, border-zinc-200 light-mode classes remain in either file
- Check-in form cards render on dark surfaces with emerald accents
- Success page renders dark with emerald confirmation icon
- All form submission, photo upload, payment verification, and routing logic unchanged
  </done>
</task>

</tasks>

<verification>
- `npx next build` passes with no errors
- Combined grep across all three files: `grep -n "bg-white\|bg-zinc-50\|bg-emerald-50\|bg-emerald-100\|bg-zinc-100\|border-zinc-200\|border-zinc-300" src/app/generating/page.tsx src/app/checkin/page.tsx src/app/success/page.tsx` returns no matches
- All API calls, form logic, animations, and routing unchanged
</verification>

<success_criteria>
All three pages are fully dark-themed: zinc-950 backgrounds, emerald-500 accents, no light-mode artifacts. The generating page has a premium Bebas Neue heading. The entire application now has 100% dark theme coverage — no light-colored pages remain.
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding/10-02-SUMMARY.md`
</output>
