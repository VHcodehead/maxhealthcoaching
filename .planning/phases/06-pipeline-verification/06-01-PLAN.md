---
phase: 06-pipeline-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/app/api/auth/signup/route.ts
  - src/app/api/admin/applications/[id]/route.ts
  - src/app/api/checkout/route.ts
  - src/app/api/checkout/verify/route.ts
  - src/app/generating/page.tsx
  - src/app/api/checkin/route.ts
  - src/app/api/admin/macro-adjustments/[id]/route.ts
  - src/app/coach/clients/[id]/page.tsx
  - src/app/api/webhooks/stripe/route.ts
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03]

must_haves:
  truths:
    - "Full client lifecycle has no broken links or missing redirects from signup through coach review"
    - "Stripe checkout creates customer, subscription, and redirects correctly through verify endpoint"
    - "All coach editing operations (meals, exercises, cardio, macros, supplements) save to DB and display on client dashboard"
    - "npm run build passes with zero errors"
    - "Prisma schema is in sync (prisma db push succeeds)"
  artifacts:
    - path: ".planning/phases/06-pipeline-verification/06-01-VERIFICATION-REPORT.md"
      provides: "Detailed audit findings and fixes for all three pipelines"
  key_links:
    - from: "src/app/api/auth/signup/route.ts"
      to: "src/middleware.ts"
      via: "subscriptionStatus=pending_approval -> /pending redirect"
      pattern: "pending_approval"
    - from: "src/app/api/admin/applications/[id]/route.ts"
      to: "src/middleware.ts"
      via: "subscriptionStatus change on approval -> pricing/checkout access"
      pattern: "subscriptionStatus"
    - from: "src/app/api/checkout/route.ts"
      to: "src/app/api/checkout/verify/route.ts"
      via: "Stripe checkout success_url includes session_id for verification"
      pattern: "session_id"
    - from: "src/app/api/checkout/verify/route.ts"
      to: "src/middleware.ts"
      via: "subscriptionStatus=active -> dashboard/onboarding access"
      pattern: "active"
    - from: "src/app/api/checkin/route.ts"
      to: "src/app/api/admin/macro-adjustments/[id]/route.ts"
      via: "Auto-adjustment created on check-in, coach reviews and approves"
      pattern: "macro-adjustments"
    - from: "src/app/coach/clients/[id]/page.tsx"
      to: "src/app/api/meal-plan/route.ts"
      via: "Coach edits save via PUT and client dashboard reads same data"
      pattern: "meal-plan"
---

<objective>
Audit every critical user flow through the codebase, tracing data and state transitions from signup to coach review. Fix any broken links, missing redirects, data flow gaps, or stale references found. Verify the build is clean.

Purpose: This is the "shake it and see what falls out" pass. Every prior phase built features independently; this plan verifies they compose into a working end-to-end product.
Output: A verification report documenting audit findings, fixes applied, and confirmation that all flows are traced and correct.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-verification/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full pipeline code audit and bug fix pass</name>
  <files>
    src/middleware.ts,
    src/app/api/auth/signup/route.ts,
    src/app/api/admin/applications/[id]/route.ts,
    src/app/api/checkout/route.ts,
    src/app/api/checkout/verify/route.ts,
    src/app/generating/page.tsx,
    src/app/api/checkin/route.ts,
    src/app/api/admin/macro-adjustments/[id]/route.ts,
    src/app/coach/clients/[id]/page.tsx,
    src/app/api/webhooks/stripe/route.ts,
    src/app/api/meal-plan/route.ts,
    src/app/api/training-plan/route.ts,
    src/app/api/supplements/route.ts,
    src/app/api/messages/route.ts,
    src/app/dashboard/page.tsx,
    .planning/phases/06-pipeline-verification/06-01-VERIFICATION-REPORT.md
  </files>
  <action>
    Perform a systematic code audit of three pipelines. For each pipeline, READ every file in the flow, trace the data/state transitions, and verify correctness. Fix any bugs found inline (do not defer).

    **Pipeline 1 — Client Lifecycle (PIPE-01):**
    Trace the full flow through code, verifying each transition:
    1. Signup: Read src/app/api/auth/signup/route.ts — verify it sets subscriptionStatus to "pending_approval" and stores application fields on Profile
    2. Pending gate: Read src/middleware.ts — verify pending_approval redirects to /pending, and /pending is in publicRoutes or handled correctly
    3. Coach approval: Read src/app/api/admin/applications/[id]/route.ts — verify PATCH changes status from pending_approval to allow checkout access (status should become "none" or similar)
    4. Checkout: Read src/app/api/checkout/route.ts — verify it creates Stripe checkout session with correct success_url containing session_id, and passes user_id in metadata
    5. Verify: Read src/app/api/checkout/verify/route.ts — verify it retrieves session, updates subscriptionStatus to "active", and sets stripeCustomerId/stripeSubscriptionId
    6. Onboarding: Read src/app/onboarding/page.tsx — verify it collects data and sets onboardingCompleted=true
    7. Plan generation: Read src/app/generating/page.tsx — verify it triggers plan generation API calls and sends plan-ready email (NOTIF-01 trigger)
    8. Dashboard: Read src/app/dashboard/page.tsx — verify it loads meals, training, supplements, messages
    9. Check-in: Read src/app/api/checkin/route.ts — verify it creates check-in record, triggers auto-adjustment if >50kcal swing, sends coach email (NOTIF-03)
    10. Auto-adjustment review: Read src/app/api/admin/macro-adjustments/[id]/route.ts — verify coach can approve, client gets notified (NOTIF-04), new macros become active

    **Pipeline 2 — Stripe Checkout (PIPE-02):**
    1. Read src/app/api/checkout/route.ts — verify checkout session creation (customer, subscription mode, success_url, cancel_url, metadata with user_id)
    2. Read src/app/api/checkout/verify/route.ts — verify session retrieval, subscription status update, correct field mapping
    3. Read src/middleware.ts — verify /dashboard gated on active subscription for clients
    4. Read src/app/api/webhooks/stripe/route.ts — verify all 4 event handlers are present and idempotent (already hardened in Phase 5, just confirm no regressions)
    5. Verify success_url in checkout matches the actual /success or post-checkout page route

    **Pipeline 3 — Coach Editing (PIPE-03):**
    1. Read src/app/coach/clients/[id]/page.tsx — identify all editing operations and their API endpoints
    2. Verify meal plan editing: Read src/app/api/meal-plan/route.ts (or admin variant) — confirm PUT handler updates meals, recalculates day totals
    3. Verify training plan editing: Read src/app/api/training-plan/route.ts (or admin variant) — confirm exercise add/remove and cardio editing
    4. Verify macro overrides: Find the macro override API and confirm it saves and reflects on client dashboard
    5. Verify supplement CRUD: Read src/app/api/supplements/route.ts — confirm create/update/delete work
    6. Verify messages: Read src/app/api/messages/route.ts — confirm send/receive/mark-read work
    7. For each editing operation, verify the client dashboard reads the same data source (no stale cache, no separate store)

    **For every bug found:**
    - Describe the bug clearly
    - Fix it in place
    - Note it in the verification report

    **Create .planning/phases/06-pipeline-verification/06-01-VERIFICATION-REPORT.md** with:
    - A section per pipeline with pass/fail status per step
    - List of bugs found and fixes applied (or "No bugs found" if clean)
    - List of warnings (not bugs, but things to watch)
  </action>
  <verify>
    <automated>cd C:/training-website && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>All three pipelines audited step-by-step. Every state transition traced. Bugs found are fixed. Verification report exists with pass/fail per step. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build and schema verification</name>
  <files>
    prisma/schema.prisma
  </files>
  <action>
    Run final build and schema checks to confirm the entire application compiles and deploys cleanly.

    1. Run `npx prisma validate` to verify schema is valid
    2. Run `npx prisma db push --accept-data-loss` with `--dry-run` if available (or just `npx prisma validate`) to confirm schema is in sync. If dry-run not available, just validate.
    3. Run `npm run build` — capture full output. Must complete with zero errors.
    4. Verify all expected routes appear in the build output:
       - /api/auth/signup
       - /api/admin/applications/[id]
       - /api/checkout
       - /api/checkout/verify
       - /api/checkin
       - /api/admin/macro-adjustments/[id]
       - /api/webhooks/stripe
       - /api/messages
       - /api/meal-plan (or admin variant)
       - /api/training-plan (or admin variant)
       - /api/supplements
       - /api/cron/checkin-reminders
    5. If build fails, fix the errors and re-run until clean. Document any fixes in the verification report from Task 1.
    6. Append build results to the verification report.
  </action>
  <verify>
    <automated>cd C:/training-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build passes with zero errors. Prisma schema validates. All expected API routes present in build output. Any build fixes documented in verification report.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with zero errors (`npx tsc --noEmit`)
- `npm run build` succeeds
- Prisma schema validates
- Verification report exists at .planning/phases/06-pipeline-verification/06-01-VERIFICATION-REPORT.md
- Report covers all three pipelines with per-step pass/fail
</verification>

<success_criteria>
- All three pipelines traced through code with no unresolved broken links or data flow gaps
- Any bugs found during audit are fixed (not deferred)
- npm run build passes with zero errors
- Verification report documents findings comprehensively
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-verification/06-01-SUMMARY.md`
</output>
