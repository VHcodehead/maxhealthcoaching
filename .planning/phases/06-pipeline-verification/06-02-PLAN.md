---
phase: 06-pipeline-verification
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03]

must_haves:
  truths:
    - "Human has walked through the full client lifecycle and confirmed it works"
    - "Human has confirmed Stripe checkout flow works in test mode"
    - "Human has confirmed coach editing operations save and display correctly on client side"
  artifacts: []
  key_links: []
---

<objective>
Human verification of every critical user flow. Max walks through the entire product end-to-end to confirm it works before the first real client is onboarded.

Purpose: Code audit (Plan 01) catches structural issues, but only a human walkthrough catches UX gaps, missing visual feedback, and flow friction. This is the final gate before launch.
Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pipeline-verification/06-CONTEXT.md
@.planning/phases/06-pipeline-verification/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full end-to-end walkthrough</name>
  <what-built>The entire coaching platform — all features from Phases 1-5 working together as one product. Plan 01 performed a code audit and fixed any structural bugs found. This checkpoint confirms everything works from a user's perspective.</what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Flow 1 — Client Lifecycle (PIPE-01):**
    1. Open an incognito window. Go to /login. Click "Apply Now" (or equivalent CTA).
    2. Fill out the coaching application form. Submit. Confirm you land on /pending with a "waiting for approval" message.
    3. Try navigating to /dashboard directly — confirm you're redirected back to /pending.
    4. Log in as coach. Go to the coach dashboard. Find the new application in the pending list. Approve it.
    5. Back in the incognito window (as the new client), refresh or navigate to /pricing. You should now have access.
    6. Complete Stripe checkout (use test card 4242 4242 4242 4242, any future expiry, any CVC).
    7. Confirm you're redirected to the post-checkout flow (success/verify page).
    8. Complete the onboarding questionnaire. Confirm macros are calculated.
    9. Trigger plan generation. Confirm the plan-ready email is sent (check Resend dashboard or logs).
    10. View the client dashboard. Confirm meals, training, supplements, and messages sections all load with real data.
    11. Submit a weekly check-in with progress photos/data. Confirm the coach gets an email notification.
    12. If auto-adjustment is triggered (>50kcal swing), confirm it creates a pending review.
    13. As coach, review and approve the macro adjustment. Confirm the client gets an email and new macros are active.

    **Flow 2 — Stripe Checkout (PIPE-02):**
    14. Verify the Stripe checkout page loaded correctly with the right plan/price.
    15. After checkout, verify subscription is "active" in your database (check via coach dashboard or Prisma Studio).
    16. Check Stripe dashboard (test mode) to confirm customer and subscription were created.

    **Flow 3 — Coach Editing (PIPE-03):**
    17. As coach, go to the client detail page. Add a new meal to a day. Confirm it appears and day totals update.
    18. Remove a meal. Confirm it's gone and totals recalculate.
    19. Add an exercise to a training day. Remove one. Confirm changes persist.
    20. Edit cardio settings (type, duration). Save. Confirm changes display correctly.
    21. Override macros for the client. Confirm new macros show on the client dashboard.
    22. Send a message to the client. Switch to client view — confirm the message appears. Reply. Confirm coach sees it with unread badge.

    **Final checks:**
    23. Confirm no console errors in browser dev tools during the entire walkthrough.
    24. Confirm no server errors in the terminal running `npm run dev`.
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found. Issues will be fixed immediately.</resume-signal>
</task>

</tasks>

<verification>
Human has walked through all three flows and either approved or provided issues to fix.
</verification>

<success_criteria>
- Human approval of the full client lifecycle flow
- Human approval of Stripe checkout flow
- Human approval of all coach editing operations
- No blocking issues remain
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-verification/06-02-SUMMARY.md`
</output>
