---
phase: 09-dashboards
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/coach/clients/[id]/page.tsx
  - src/components/coach/coach-supplements.tsx
  - src/components/coach/coach-notes.tsx
  - src/components/coach/client-messages.tsx
  - src/components/coach/edit-meal-dialog.tsx
  - src/components/coach/edit-training-day-dialog.tsx
  - src/components/coach/macro-override-form.tsx
  - src/components/coach/pending-macro-review.tsx
autonomous: true
requirements: [COACH-03]

must_haves:
  truths:
    - "Coach client detail page uses dark expandable sections with zinc-900 headers"
    - "All editing dialogs are dark themed with distinct edit vs read states"
    - "Coach notes, supplements, and messages components are dark themed"
    - "No light-mode color classes remain in any file"
  artifacts:
    - path: "src/app/coach/clients/[id]/page.tsx"
      provides: "Dark client detail page with expandable sections"
      contains: "bg-card"
    - path: "src/components/coach/coach-supplements.tsx"
      provides: "Dark supplement management"
      contains: "bg-card"
    - path: "src/components/coach/edit-meal-dialog.tsx"
      provides: "Dark meal editing dialog"
      contains: "bg-card"
    - path: "src/components/coach/edit-training-day-dialog.tsx"
      provides: "Dark training editing dialog"
      contains: "bg-card"
  key_links:
    - from: "src/app/coach/clients/[id]/page.tsx"
      to: "src/components/coach/*"
      via: "component imports"
      pattern: "import.*components/coach"
---

<objective>
Restyle the coach client detail page and all coach components to the dark athletic hybrid theme.

Purpose: The client detail page is the coach's primary workspace — it must be fully dark-themed with clear visual distinction between read and edit states. This is the largest single page (~1500 lines) plus 7 component files (~2000 lines).
Output: 8 restyled files — the complete coach editing experience.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboards/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark restyle coach client detail page</name>
  <files>src/app/coach/clients/[id]/page.tsx</files>
  <action>
This is a large file (~1488 lines). Apply ALL global color replacements systematically:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

Section-specific styling per CONTEXT decisions:
- Page title (client name): add `font-display` class
- Expandable section headers: `bg-card` (zinc-900)
- Expandable section content areas: `bg-zinc-950` or `bg-background`
- Stat cards within sections: `bg-card` with colored left borders
- Stat values: add `font-display` class
- Meal plan sections: dark cards matching DASH-03 pattern (emerald day tabs, colored macro badges)
- Training sections: dark tables matching DASH-04 pattern (emerald gradient headers, alternating rows)
- Macro overview: dark cards with progress bars on `bg-zinc-800` tracks
- Edit buttons/actions: ensure they are visually distinct from read state — use `bg-primary` or `border-primary` styling
- Any inline forms: dark input styling
- Tab navigation within sections: dark tab styling

IMPORTANT: Preserve ALL functionality — this page has complex state management, editing flows, and many interactive elements. Only change visual classes, never logic.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Client detail page is fully dark themed with expandable sections, dark stat cards with font-display values, and visually distinct edit states. No light-mode classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Dark restyle all coach component files</name>
  <files>src/components/coach/coach-supplements.tsx, src/components/coach/coach-notes.tsx, src/components/coach/client-messages.tsx, src/components/coach/edit-meal-dialog.tsx, src/components/coach/edit-training-day-dialog.tsx, src/components/coach/macro-override-form.tsx, src/components/coach/pending-macro-review.tsx</files>
  <action>
**For ALL 7 component files**, apply global color replacements:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

**coach-supplements.tsx (~605 lines):**
- Supplement cards: `bg-card` with `border-white/10`
- Category badges: bright colors on dark — `text-amber-400`, `text-slate-400`, `text-red-400`
- Timing sections: `bg-card`
- Any dropdown/select: ensure dark styling

**coach-notes.tsx (~196 lines):**
- Notes cards: `bg-card`
- Note input/textarea: dark input styling
- Timestamps: `text-muted-foreground`

**client-messages.tsx (~218 lines):**
- Message thread: `bg-card`
- Coach bubbles: `bg-emerald-900/50`
- Client bubbles: `bg-zinc-800`
- Message input: dark styling
- Timestamps: `text-muted-foreground`

**edit-meal-dialog.tsx (~238 lines):**
- Dialog background: `bg-card` (Phase 7 dialog override should handle this, but verify no inline overrides)
- Form inputs: dark input styling
- Macro display: colored text matching DASH-03 pattern

**edit-training-day-dialog.tsx (~424 lines):**
- Dialog background: `bg-card`
- Exercise list: dark rows with `border-white/10`
- Form inputs: dark input styling
- Set/rep fields: dark styling

**macro-override-form.tsx (~153 lines):**
- Form container: `bg-card`
- Input fields: dark input styling
- Current vs new values: distinct colors on dark

**pending-macro-review.tsx (~217 lines):**
- Review card: `bg-card`
- Pending indicator: `bg-amber-900/20 text-amber-400`
- Approve/reject buttons: emerald for approve, destructive for reject
- Macro comparison: clear colors on dark background

IMPORTANT: Preserve ALL functionality in every component. Only change visual classes.
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 coach components are dark themed. Message bubbles have emerald coach / zinc client distinction. Edit dialogs are dark. No light-mode classes remain in any component file.</done>
</task>

</tasks>

<verification>
After both tasks, verify no light-mode artifacts remain:
```bash
grep -rn "bg-white\|bg-gray-50\|bg-zinc-50\|text-zinc-900\|text-gray-900\|border-zinc-200\|border-gray-200" src/app/coach/clients/\[id\]/page.tsx src/components/coach/
```
Should return no results. Build must pass.
</verification>

<success_criteria>
- All 8 files use dark semantic CSS variables exclusively
- font-display applied to client name and stat values on detail page
- Coach message bubbles are emerald, client bubbles are zinc
- Edit states visually distinct from read states
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboards/09-04-SUMMARY.md`
</output>
