---
phase: 09-dashboards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/supplements/page.tsx
  - src/app/dashboard/messages/page.tsx
  - src/app/dashboard/progress/page.tsx
  - src/app/dashboard/referral/page.tsx
autonomous: true
requirements: [DASH-05]

must_haves:
  truths:
    - "Supplements page has dark timing cards with bright category badge colors"
    - "Messages page has dark thread with emerald coach bubbles and zinc client bubbles"
    - "Progress page has dark check-in history cards"
    - "Referral page is dark themed and consistent"
  artifacts:
    - path: "src/app/dashboard/supplements/page.tsx"
      provides: "Dark supplements page"
      contains: "bg-card"
    - path: "src/app/dashboard/messages/page.tsx"
      provides: "Dark messages with colored bubbles"
      contains: "emerald-900"
    - path: "src/app/dashboard/progress/page.tsx"
      provides: "Dark progress cards"
      contains: "bg-card"
    - path: "src/app/dashboard/referral/page.tsx"
      provides: "Dark referral page"
      contains: "bg-card"
  key_links:
    - from: "src/app/dashboard/messages/page.tsx"
      to: "globals.css"
      via: "semantic CSS variables"
      pattern: "bg-card|emerald-900"
---

<objective>
Restyle the client supplements, messages, progress, and referral pages to the dark athletic hybrid theme.

Purpose: Complete the client dashboard dark restyle — every page a client visits is on-brand.
Output: 4 restyled secondary client dashboard pages.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboards/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark restyle supplements and messages pages</name>
  <files>src/app/dashboard/supplements/page.tsx, src/app/dashboard/messages/page.tsx</files>
  <action>
**For BOTH files**, apply global color replacements:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

**supplements/page.tsx (DASH-05):**
- Page title: add `font-display` class
- Timing cards (morning, afternoon, evening, etc.): `bg-card`
- Category badge colors brightened for dark backgrounds: use `text-amber-400`, `text-slate-400`, `text-red-400` etc. instead of darker variants
- Supplement item cards: `bg-card` with `border-white/10`

**messages/page.tsx (DASH-05):**
- Page title: add `font-display` class
- Message thread container: `bg-card`
- Coach message bubbles: `bg-emerald-900/50` — like iMessage dark mode
- Client message bubbles: `bg-zinc-800`
- Message input area: ensure it uses dark input styling
- Timestamps: `text-muted-foreground`
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Supplements page has dark timing cards with bright badge colors. Messages page has emerald coach bubbles and zinc client bubbles on dark background. No light-mode color classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Dark restyle progress and referral pages</name>
  <files>src/app/dashboard/progress/page.tsx, src/app/dashboard/referral/page.tsx</files>
  <action>
**For BOTH files**, apply global color replacements:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

**progress/page.tsx (DASH-05):**
- Page title: add `font-display` class
- Check-in history cards: `bg-card`
- Photo comparison sections: dark background, ensure images have proper contrast
- Stats/metrics within check-ins: `font-display` on key numbers
- Any data tables or lists: dark styling consistent with training page pattern
- Status indicators: bright colors on dark (emerald for positive, amber for warning)

**referral/page.tsx (DASH-05):**
- Page title: add `font-display` class
- Referral code card: `bg-card` with emerald accent
- Stats (referral count etc.): `font-display` on numbers
- Any CTA buttons: ensure they use `bg-primary` styling
- All card sections: `bg-card` with `border-white/10`
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Progress page has dark check-in cards. Referral page is dark themed. No bg-white, bg-gray-50, text-zinc-900, or border-zinc-200 remain in either file.</done>
</task>

</tasks>

<verification>
After both tasks, verify no light-mode artifacts remain:
```bash
grep -n "bg-white\|bg-gray-50\|bg-zinc-50\|text-zinc-900\|text-gray-900\|border-zinc-200\|border-gray-200" src/app/dashboard/supplements/page.tsx src/app/dashboard/messages/page.tsx src/app/dashboard/progress/page.tsx src/app/dashboard/referral/page.tsx
```
Should return no results. Build must pass.
</verification>

<success_criteria>
- All 4 files use dark semantic CSS variables exclusively
- font-display applied to page titles and key stat values
- Message bubbles have distinct coach (emerald) vs client (zinc) styling
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboards/09-02-SUMMARY.md`
</output>
