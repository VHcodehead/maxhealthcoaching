---
phase: 09-dashboards
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/coach/page.tsx
  - src/app/coach/clients/page.tsx
  - src/app/coach/settings/page.tsx
  - src/app/coach/content/page.tsx
autonomous: true
requirements: [COACH-01, COACH-02]

must_haves:
  truths:
    - "Coach overview has dark stat cards with colored left borders and font-display values"
    - "Coach client list shows dark cards with bright status badges and unread indicators"
    - "Coach settings and content pages are dark themed and consistent"
  artifacts:
    - path: "src/app/coach/page.tsx"
      provides: "Dark coach overview with stat cards"
      contains: "font-display"
    - path: "src/app/coach/clients/page.tsx"
      provides: "Dark client list with status badges"
      contains: "bg-card"
    - path: "src/app/coach/settings/page.tsx"
      provides: "Dark settings page"
      contains: "bg-card"
    - path: "src/app/coach/content/page.tsx"
      provides: "Dark content management page"
      contains: "bg-card"
  key_links:
    - from: "src/app/coach/page.tsx"
      to: "globals.css"
      via: "semantic CSS variables"
      pattern: "bg-card|font-display"
---

<objective>
Restyle the coach overview, client list, settings, and content pages to the dark athletic hybrid theme.

Purpose: Coach-facing pages must feel equally premium — consistent with client dashboard styling.
Output: 4 restyled coach dashboard pages.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboards/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark restyle coach overview and client list pages</name>
  <files>src/app/coach/page.tsx, src/app/coach/clients/page.tsx</files>
  <action>
**For BOTH files**, apply global color replacements:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

**page.tsx — Coach Overview (COACH-01):**
- Page title: add `font-display` class
- Stat cards: `bg-card` with colored left borders (same pattern as client — emerald, blue, violet, amber)
- Stat values: add `font-display` class for Bebas Neue
- Pending applications card: `bg-card` with `border-l-4 border-amber-500`, "Why now?" blockquote in `bg-zinc-800`
- Recent activity feed: `bg-card` items with `border-b border-white/10` separators
- Quick action cards: `bg-card` with `hover:border-primary` emerald hover

**clients/page.tsx — Client List (COACH-02):**
- Page title: add `font-display` class
- Client cards: `bg-card`
- Status badges — bright on dark: `text-emerald-400` for "active", `text-amber-400` for "overdue", `text-zinc-400` for "pending"
- Unread message pills: `bg-primary text-primary-foreground` (emerald-500 with white text)
- Search/filter inputs: ensure dark input styling
- Any tables or grids: dark styling with `border-white/10`
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Coach overview has dark stat cards with font-display values and colored borders. Client list has dark cards with bright status badges. No light-mode classes remain.</done>
</task>

<task type="auto">
  <name>Task 2: Dark restyle coach settings and content pages</name>
  <files>src/app/coach/settings/page.tsx, src/app/coach/content/page.tsx</files>
  <action>
**For BOTH files**, apply global color replacements:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

**settings/page.tsx:**
- Page title: add `font-display` class
- Settings sections/cards: `bg-card`
- Form inputs: dark input styling (should inherit from Phase 7 but verify no inline overrides)
- Save buttons: `bg-primary` styling
- Section dividers: `border-white/10`

**content/page.tsx:**
- Page title: add `font-display` class
- Content cards/sections: `bg-card`
- Any tables: dark table styling with `bg-muted` headers
- Action buttons: ensure primary styling
- Any code/template blocks: `bg-zinc-800` or `bg-zinc-950`
- Tab navigation (if any): dark tab styling matching Phase 7 overrides
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Settings and content pages are dark themed. No bg-white, bg-gray-50, text-zinc-900, or border-zinc-200 remain in either file.</done>
</task>

</tasks>

<verification>
After both tasks, verify no light-mode artifacts remain:
```bash
grep -n "bg-white\|bg-gray-50\|bg-zinc-50\|text-zinc-900\|text-gray-900\|border-zinc-200\|border-gray-200" src/app/coach/page.tsx src/app/coach/clients/page.tsx src/app/coach/settings/page.tsx src/app/coach/content/page.tsx
```
Should return no results. Build must pass.
</verification>

<success_criteria>
- All 4 files use dark semantic CSS variables exclusively
- font-display applied to page titles and stat values
- Status badges use bright colors on dark backgrounds
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboards/09-03-SUMMARY.md`
</output>
