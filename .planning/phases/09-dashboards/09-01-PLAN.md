---
phase: 09-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/layout.tsx
  - src/app/dashboard/page.tsx
  - src/app/dashboard/meals/page.tsx
  - src/app/dashboard/training/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "Client sidebar is dark zinc-900 with emerald active nav states — no white or light sidebar visible"
    - "Client overview stat cards are on dark surfaces with font-display stat values"
    - "Meal plan page uses dark cards with emerald active day tabs"
    - "Training page uses dark exercise tables with emerald gradient headers"
  artifacts:
    - path: "src/app/dashboard/layout.tsx"
      provides: "Dark sidebar with emerald active states"
      contains: "bg-card"
    - path: "src/app/dashboard/page.tsx"
      provides: "Dark overview with stat cards"
      contains: "font-display"
    - path: "src/app/dashboard/meals/page.tsx"
      provides: "Dark meal plan cards"
      contains: "bg-card"
    - path: "src/app/dashboard/training/page.tsx"
      provides: "Dark training tables"
      contains: "bg-card"
  key_links:
    - from: "src/app/dashboard/layout.tsx"
      to: "globals.css"
      via: "semantic CSS variables"
      pattern: "bg-card|bg-background|text-foreground"
---

<objective>
Restyle the client dashboard layout, overview, meals, and training pages to the dark athletic hybrid theme.

Purpose: Core client experience pages must feel premium — dark surfaces, emerald accents, bold typography.
Output: 4 restyled client dashboard pages using Phase 7 semantic CSS variables.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboards/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark restyle client sidebar layout and overview page</name>
  <files>src/app/dashboard/layout.tsx, src/app/dashboard/page.tsx</files>
  <action>
**layout.tsx (DASH-01):**
Apply global color replacements throughout the file:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

Sidebar-specific styling per CONTEXT decisions:
- Sidebar background: `bg-card` (resolves to zinc-900)
- Sidebar border: `border-r border-white/10`
- Active nav item: emerald-500 background with white text (use `bg-primary text-primary-foreground` or check if sidebar-primary vars are set)
- Inactive nav items: `text-zinc-400 hover:text-zinc-300`
- User section at bottom: `bg-zinc-800`
- Mobile sheet: `bg-card` background
- Mobile header bar: `bg-background` with `border-b border-white/10`

**page.tsx (DASH-02):**
Apply global color replacements throughout the file.
- Page title: add `font-display` class
- Stat cards: `bg-card` background, keep existing colored left borders (emerald, blue, violet)
- Stat values (large numbers): add `font-display` class for Bebas Neue
- Macro breakdown section: `bg-card`, progress bar tracks `bg-zinc-800`, keep emerald/blue/amber/rose fills
- Meal/training preview cards: `bg-card`
- "Download Plan" card: `bg-card` with emerald dashed border (`border-dashed border-primary`)
- Email verification banner: `bg-amber-900/20 text-amber-400` instead of any light amber styling
- All hover states: ensure they work on dark surfaces
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Client sidebar is dark with emerald active states. Overview page has dark stat cards with font-display on values. No bg-white, bg-gray-50, text-zinc-900, or border-zinc-200 remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Dark restyle client meals and training pages</name>
  <files>src/app/dashboard/meals/page.tsx, src/app/dashboard/training/page.tsx</files>
  <action>
**meals/page.tsx (DASH-03):**
Apply global color replacements throughout the file:
- Replace all `bg-white`, `bg-gray-50`, `bg-zinc-50` with `bg-card` or `bg-background`
- Replace all `text-zinc-900`, `text-gray-900` with `text-foreground`
- Replace all `text-zinc-500`, `text-gray-500` with `text-muted-foreground`
- Replace all `border-zinc-200`, `border-gray-200` with `border-border`

Meals-specific styling per CONTEXT decisions:
- Page title: add `font-display` class
- Day tabs: inactive `bg-zinc-800`, active `bg-primary text-primary-foreground` (emerald-500 with white text)
- Meal cards: `bg-card` with `border-white/10` borders
- Macro badges: filled dark variants — `bg-emerald-500/10 text-emerald-400` for calories, `bg-blue-500/10 text-blue-400` for protein, `bg-amber-500/10 text-amber-400` for carbs, `bg-rose-500/10 text-rose-400` for fat
- Day totals card: `bg-emerald-900/20 text-emerald-400` instead of any emerald-50 styling
- Grocery list sections: `bg-card` accordion cards
- Any swap dialog: `bg-card` background (already handled by Phase 7 dialog override, but verify inline styles)

**training/page.tsx (DASH-04):**
Apply global color replacements throughout the file.
- Page title: add `font-display` class
- Week tabs: same pattern as meal day tabs — inactive `bg-zinc-800`, active `bg-primary text-primary-foreground`
- Training day cards: `bg-card` with emerald gradient header — add `bg-gradient-to-r from-emerald-950/50 to-transparent` to card headers
- Exercise table: header `bg-muted`, alternating rows `bg-card`/`bg-zinc-950`, borders `border-white/10`
- Warmup section: `bg-orange-900/20 text-orange-400`
- Cardio section: `bg-blue-900/20 text-blue-400`
- Exercise number circles: `bg-primary text-primary-foreground` (emerald-500 with white text)
- All hover states: ensure they work on dark surfaces
  </action>
  <verify>
    <automated>cd C:/training-website && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Meals page has dark cards with emerald day tabs and colored macro badges. Training page has dark tables with emerald gradient headers. No bg-white, bg-gray-50, text-zinc-900, or border-zinc-200 remain in either file.</done>
</task>

</tasks>

<verification>
After both tasks, verify no light-mode artifacts remain:
```bash
grep -n "bg-white\|bg-gray-50\|bg-zinc-50\|text-zinc-900\|text-gray-900\|border-zinc-200\|border-gray-200" src/app/dashboard/layout.tsx src/app/dashboard/page.tsx src/app/dashboard/meals/page.tsx src/app/dashboard/training/page.tsx
```
Should return no results. Build must pass.
</verification>

<success_criteria>
- All 4 files use dark semantic CSS variables exclusively
- font-display applied to page titles and stat values
- Emerald accent on active tabs, colored borders, gradient headers
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboards/09-01-SUMMARY.md`
</output>
